# Faster-LIO (ROS1 / Noetic) config for MandEye datasets

# Input topics in the ROS1 bag (MandEye -> rosbag1)
INPUT_LIDAR_TOPIC=/livox/lidar
INPUT_IMU_TOPIC=/livox/imu

# Where to store outputs (created under the experiment directory)
OUTPUT_DIR=results-fasterlio

# Topics produced by Faster-LIO to record as an intermediate bag
RECORD_TOPICS="/cloud_registered /Odometry"

# Which launch file to run in the container (package + launch)
FASTERLIO_LAUNCH="faster_lio mapping_avia.launch"

# Extra args passed to rosbag play (keep --clock for /use_sim_time setups)
ROSPLAY_ARGS="--clock"

